<% if @next %>
<div class="p-6 max-w-xl mx-auto">
<h1 class="text-xl font-bold mb-4 text-center text-blue-600">(Thème <%= @theme_position %> / <%= @total_themes %>) <%= @current_theme.name %></h1>
<h2 class="text-lg font-semibold mb-2">(Question <%= @question_position %> / <%= @total_questions_in_theme %>)</h2>
<p class="mb-4"><%= @next.label %></p>
<%= form_with url: answer_path(token: @participation.token), method: :post do |f| %>
<%= hidden_field_tag :question_id, @next.id %>
<div class="grid grid-cols-5 gap-2 mb-4">
<% (1..10).each do |n| %>
<button name="score" value="<%= n %>" class="border border-gray-300 rounded px-3 py-2 bg-white hover:bg-blue-50 hover:border-blue-300 focus:bg-blue-100 focus:border-blue-500 focus:ring-2 focus:ring-blue-200 transition-colors duration-200"><%= n %></button>
<% end %>
</div>
<% end %>
</div>
<% end %>